/**
* Invalid KeyQL Query with Options
* @param {object.keyql.order} order Order for keyql {?} ["status", null, "goodbye"]
* @returns {string}
*/
module.exports = async (order) => {
  return 'hello';
};
