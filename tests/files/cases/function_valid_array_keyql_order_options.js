/**
* Valid KeyQL Query with Options
* @param {array} order
* @ {object.keyql.order} orderEntry Order for keyql {?} ["status", "color", "goodbye"]
* @returns {string}
*/
module.exports = async (order) => {
  return 'hello';
};
