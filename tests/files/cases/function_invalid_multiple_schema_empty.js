/**
* Accepts multiple schemas for an object
* @param {object} fileOrFolder
* @ OR
* @ {string} name
* @ {number} size
* @returns {object}
*/
module.exports = async (fileOrFolder) => {
  return true;
};
