/**
* Valid KeyQL Query with Options
* @param {object.keyql.order} order Order for keyql {?} ["status", "color", "goodbye"]
* @returns {string}
*/
module.exports = async (order) => {
  return 'hello';
};
