/**
* Valid function for origins
* @origin autocode.com
* @origin localhost:8000
* @origin test.some-url.com:9999
* @origin https://hello.com
* @background
* @param {string} alpha Some value
* @stream {boolean} hello Some value
*/
module.exports = async (alpha, context) => {

  return true;

};
